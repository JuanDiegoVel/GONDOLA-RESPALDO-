[pytest]
# `pythonpath` hace que `import gondola` funcione desde la raiz del repo sin
# instalar nada ni tocar variables de entorno. Se corre siempre asi:
#     pytest
pythonpath = ai-service
testpaths = ai-service/tests
